apiVersion: v1
kind: PersistentVolume
metadata:
  name: ebs-static
spec:
  accessModes:
  - ReadWriteOnce
  capacity:
    storage: 30GI
  csi:
    driver: ebs.csi.aws.com
    fsType: ext4
    volumeHandle: vol-032758129002cc122
