kind: ReplicaSet
apiversion: apps/v1
metadata:
  name: nginx
  labels:
    app: guestbook
    tier: frontend
spec:
  replicas: 3
  selelctor:
    matchLabels:
      tier: frontend
  template:
    metadata:
      labels:
        tier: frontend
    spec:
      containers:
      - name: php-redis
      image: